# Test Project - Ansible CD

This fixture models a small VM deployment. CI uses command mocks from
`mock-bin` so the action can exercise setup, playbook execution, and status
logging without opening SSH connections.

